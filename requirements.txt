anthropic>=0.40.0
pydantic-settings>=2.5.0

# --- Web senses (search + scrape) ---
httpx>=0.27.0
beautifulsoup4>=4.12.0
duckduckgo-search>=6.0.0

# --- Model Context Protocol (Tavily search over a stdio MCP server) ---
# `mcp` is the official Python SDK; our thin server in `mcp_integration/`
# spawns as a stdio subprocess and exposes a single `tavily_search` tool.
mcp>=1.2.0
tavily-python>=0.5.0

# --- Enterprise memory (RAG + embeddings) ---
openai>=2.46.0
# NOTE: The official Pinecone SDK was renamed from `pinecone-client` to
# `pinecone` in the 5.x line. The 6.x `pinecone-client` package is now a
# no-op shim that raises on import, so we depend on `pinecone` directly.
pinecone>=5.0.0
